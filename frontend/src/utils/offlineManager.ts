export async function downloadLessonFile(id: string, url: string, onProgress: (p: number) => void) {
  return null;
}

export async function getLocalUri(url: string) {
  return null;
}

export async function isLessonDownloaded(id: string) {
  return false;
}

export async function deleteLessonFile(id: string) {
  return;
}
